# Server encryption key for API key storage (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=your-64-char-hex-key-here

# Server port (default: 3001)
PORT=3001

# Web Search API Keys (Optional: falls back to DuckDuckGo if omitted)
TAVILY_API_KEY=your-tavily-api-key-here

# Optional provider keys for environment-based seeding:
# EXPERIENTIAL_API_KEY=xpl_your_key_here
