{
  "name": "freellmapi",
  "description": "A self-hosted, OpenAI-compatible proxy routing across 11+ free-tier LLM providers (Gemini, Groq, Cerebras, etc.) with smart failover and encrypted key storage.",
  "homepage": "https://byteWizard-zero.github.io/my-freellmapi-proxy/",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "test": "npm run test -w server && npm run test -w client",
    "build": "npm run build -w server && npm run build -w client",
    "build:server": "npm run build -w server",
    "start": "npm run start -w server",
    "sync-keys": "node scripts/sync-sibling-keys.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
